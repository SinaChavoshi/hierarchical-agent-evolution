{
  "task_id": "self-hosting-artifacts",
  "objective": "Implement the artifact-filtering module for the Hierarchical Agent Evolution platform. Your submission is graded by running a held-out test suite against the module you write. Nothing you write outside the target module is read, and no part of the score is awarded for describing what you built.",
  "benchmark_task": "artifacts",
  "budget_usd": 1.50,
  "max_calls": 120,
  "capabilities": ["workspace.read", "workspace.write", "workspace.shell", "workspace.verify"],
  "max_iterations": 1,
  "carry_artifacts": false,
  "metadata": {
    "set": "v2",
    "notes": "V2 Generation 1. carry_artifacts stays false for the first generation so its fitness is comparable to the V1 single-shot record; the A/B against carryover is Generation 2's job."
  }
}
